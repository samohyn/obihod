# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log

# Local env (each subproject имеет свой .gitignore)
.env.local

# Claude Code — локальные логи хуков и local-only настройки
.claude/memory/logs/
.claude/settings.local.json
.claude/secrets/

# graphify — кэш не коммитим, сам граф (graph.json, GRAPH_REPORT.md, graph.html) коммитим
graphify-out/cache/
graphify-out/transcripts/

# Playwright скриншоты — локально, не коммитить
screen/

# Heavy media assets — не коммитим исходники, только мета и README
assets/photos/
assets/videos/
assets/temp/
